# Loop Profiler - Requirements
# ============================================================

# 必須ライブラリ
PyQt6==6.7.0
pydub==0.25.1
pymusiclooper==3.4.1
mutagen>=1.47.0

# AI機能用（オプション）
# インストール: pip install -r requirements.txt
# または個別に: pip install librosa scikit-learn
scikit-learn==1.3.0
librosa==0.10.2

# librosaの依存関係（自動的にインストールされますが明示）
numpy>=1.24.0,<2.0.0
scipy>=1.10.0
soundfile>=0.12.0

# ============================================================
# インストール方法
# ============================================================
# 【基本機能のみ】
#   pip install PyQt6 pydub pymusiclooper mutagen
#
# 【AI機能を含む（推奨）】
#   pip install -r requirements.txt
#
# 【AI機能が不要な場合】
#   AI機能なしでも全ての基本機能（ループ検出、プレビュー、エクスポート）は動作します。
# ============================================================

# Loop Profiler - Requirements
# ============================================================

# 既存の必須ライブラリ
PyQt6==6.7.0
pydub==0.25.1
pymusiclooper==3.4.1

# AI機能用（推奨）
# インストール: pip install -r requirements.txt
# または個別に: pip install librosa scikit-learn
scikit-learn==1.3.0
librosa==0.10.2

# librosaの依存関係（自動的にインストールされますが明示）
numpy>=1.24.0,<2.0.0
scipy>=1.10.0
soundfile>=0.12.0

# ============================================================
# インストール方法:
# ============================================================
# 【基本機能のみ】
#   pip install PyQt6 pydub pymusiclooper
#
# 【AI機能を含む（推奨）】
#   pip install -r requirements.txt
#
# 【AI機能が不要な場合】
#   AI機能なしでも全ての基本機能（ループ検出、プレビュー、
#   エクスポート）は動作します。
# ============================================================
